fun main(){
    // functions they are a block of statement that performs a given task
    // A function is introduced by the use of the fun key word followed by the name of function, parenthesis and the body of the function come last which is introduced by the use of curlybraces.

    fun sayHello () {
        println("Hello there, Hope you are a having a good day?")
    }

    // Below we invoke/call the function
    sayHello()

    println("============================================================================================")

    // create your own example of a function below and also invoke it
    
    fun showMe(){
        println("You got the new Porsche available?")
    }
    showMe()
}