fun main(args: Array<String>){
// create assignment3.kt and by use of for loop, create an array of 7 counties and loop through.

val counties = arrayOf("Nairobi", "Mombasa", "Kisumu", "Nakuru", "Uasin Gishu", "Machakos", "Kiambu")

for (county in counties) {
    if (index == 0 || index == 2) {
        println(counties)
    }

    
}
// println("First: ${counties[0]}, Third: ${counties[2]}")

println("============================================================================================")

// Create a for loop to print year from 2000 to 2025
for (year in 2000..2025) {
    println(year)
}
}